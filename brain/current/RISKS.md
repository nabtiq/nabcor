# Risk Register

**Updated:** 2026-07-20
Material risks only. Review at every phase boundary.

| ID | Risk | Probability | Impact | Early signal | Primary mitigation | Owner role | Status |
|---|---|---:|---:|---|---|---|---|
| RISK-PROD-01 | Output is polished but generic. | medium | critical | humans cannot distinguish territories from template output | bounded diversity experiment, recorded rejection, calibrated evaluation | product owner | open |
| RISK-TRUST-01 | Fabricated claim or misleading generated asset reaches a client. | low–medium | critical | unbacked factual slots or provisional content loses its label | provenance contracts, deterministic claim gate, human approval | operator | open |
| RISK-EVAL-01 | Model judges create false numerical authority. | high | high | score/human preference disagreement | advisory status until calibrated; reasons and evidence mandatory | evaluation owner | open |
| RISK-SCOPE-01 | The clean repository accumulates channels and frameworks before the slice works. | high | high | new runtime/UI proposals without exit evidence | Constitution feature test, decision records, NON_GOALS | product owner | open |
| RISK-COST-01 | Token and cache spend grows without improving surviving artifacts. | medium | high | missing run records or frequent budget breaches | budgets, context manifests, survival metrics, EXP-0005 | operator | open |
| RISK-MEM-01 | Second Brain becomes stale documentation theatre. | medium | high | decisions remain in chat; NOW is outdated; learnings are not loaded | session bootstrap, structural validation, phase-boundary review | product owner | open |
| RISK-ARCH-01 | Schemas and consuming code drift. | medium | high | schema bump without migration/test changes | versioned contracts, negative fixtures, CI | architecture owner | open |
| RISK-DATA-01 | Client material crosses brands or enters evaluation/training without rights. | low–medium | critical | missing rights records or shared content namespaces | default-deny rights, per-brand isolation, provider approvals | operator | open |
| RISK-PROVIDER-01 | Product behavior couples to one provider/model. | medium | medium | skills import provider SDKs directly; Anthropic-specific logic outside the adapter boundary | provider-neutral gateway with fail-closed policy contract (DEC-0009/DEC-0010, migrated by DEC-0019), capability tiers, provider-independence grep gate confining network capability to the single pinned transport module | architecture owner | open |
| RISK-LIVE-01 | The general-live-disabled boundary (DEC-0019, preserved by DEC-0020) erodes: a future change enables GENERAL live invocation, executes EXP-0001, or relaxes a ceiling without its own ceremony — or documentation claims the provider is "live"/"operational" when only one consumed smoke authorization has ever been used. Phase 1C.2 made exactly one reconciled smoke request; the authorization is consumed and non-replayable, and general live invocation stays off. | low | critical | any edit to provider-operational-state or the gateway policy outside a ratified phase; a second live request without a new signed authorization; docs claiming general live/operational use or an EXP-0001 result with no run record and reconciliation evidence | general `live_invocation_enabled` and `exp_0001_executed` are schema CONSTANTS false in every state (flipping one requires a contract migration + decision); the active policy is byte-pinned in CI; the candidate/evidence/authority/policy chain is CI-re-verified including the Ed25519 signature; any further live call needs a NEW unconsumed signed live-call authorization (the smoke one is consumed, proven by a live replay drill); tripwire scans block "provider is enabled/live" claims | product owner | open |
| RISK-SECRET-01 | The dedicated Anthropic API credential provisioned in Phase 1C.2 is now a standing secret in the macOS Keychain (service `nabcor-anthropic-api-key`, account `nabcor`). Exposure would allow spend against the NABCor account up to the USD 60 monthly hard cap; leakage into a tracked file, log, or CI artifact would be a disclosure. | low | high | the credential value appearing in any tracked file, commit, log line, run record, result artifact, or CI secret; unexpected console spend or request count above the one reconciled smoke call; the USD 60 hard cap or auto-reload setting changing | the key lives only in the Keychain, never on disk in the clear and never committed; the secret resolver reads it only after every non-secret gate and emits zero secret-shaped diagnostics; a repository-wide leakage scan runs in the test suite; the USD 60 provider-console hard cap bounds worst-case spend and auto-reload is disabled; rotation/revocation is the Product Owner's responsibility on any suspected exposure | product owner | open |
| RISK-OPS-01 | Uncommitted or session-only work is lost. | medium | high | long work spans without canonical artifacts/commits | durable workspace, coherent checkpoints, append-only learning | operator | mitigated |
| RISK-KEY-01 | Human-gate private key material is exposed, stolen, lost, expired, or enrolled without review (DEC-0014/DEC-0015). One real Product Owner key is now enrolled (registry v2, expiry 2027-07-19): loss stalls approvals until rotation, theft could sign ordinary fact-resolution approvals until revocation — and since DEC-0016 a stolen-key approval over a prepared decision would APPLY a fact resolution (immutable, auditable, reversible only by a new signed resolution), so revocation urgency is higher — and expiry fails closed. | low | critical | private material in any tracked file, log, fixture, or CI artifact; a registry revision without a decision record; an approval verifying against an unreviewed key; an approval signed after suspected compromise; the 2027-07-19 expiry approaching without a rotation decision | private keys never committed/logged/fixtured (repository-wide leakage scan in the test suite); keygen and signing CLIs refuse in-repo/symlink/insecure paths and print no private material; the signing CLI derives identity from the key and refuses unenrolled key IDs; enrollment/rotation/revocation each require a reviewed registry revision + policy pin ratified by a decision record (DEC-0015 is the enrollment evidence); secure encrypted backup is the Product Owner's confirmed personal responsibility; loss/theft recovery is rotation + revocation — never bypass; expiry and revocation fail closed and history is immutable | product owner | open |
| RISK-CONC-01 | The fact-resolution application (DEC-0016) relies on single-host/single-writer file atomicity: between its preflight and its successor writes there is no cross-process lock, so a concurrent writer in the same claim namespace can force a fail-closed partial state (stale snapshot, ambiguous fork) that needs a fresh decision and signature to finish the remaining conflict. | low | medium | resolution attempts repeatedly failing with stale-analysis or resolution-conflict; any deployment plan with more than one writer host | fail-closed guards (aggregate digest currency, fork refusal, byte-exact resume) make the race lose availability, never correctness; a multi-writer or multi-host deployment requires a new decision with real coordination (DEC-0016 revisit trigger) | architecture owner | open |
| RISK-DECAY-01 | The DEC-0018 evidence base decays: provider prices, retention policies, model lifecycles, and terms drift after the 2026-07-19 access date, so a late ratification would rest on stale facts (e.g., Sonnet 5 intro pricing ends 2026-08-31; Haiku 4.5 retirement floor 2026-10-15; Gemini 2.5 shutdown 2026-10-16). | medium | medium | ratification attempted >60 days after the access date; a provider announces pricing/retention/lifecycle changes | the packet and DEC-0018 both carry the re-verification rule (re-check all sources before late ratification); every fact carries its source URL and access date so re-verification is mechanical; the cost model already prices post-intro rates | product owner | open |
| RISK-CLI-01 | An operator drives the wrong namespace, stale state, or wrong artifact through the convenience of the CLI (DEC-0017), or a future CLI change quietly grows into a second implementation of domain logic. | low–medium | medium | a mutation succeeding without a digest-bound confirmation; CLI output diverging from stored artifacts; domain logic appearing in src/cli/ | digest-bound `--confirm-digest` on every mutation (dry-run/inspect first), explicit roots and namespaces with no ambient defaults, thin-orchestration rule enforced by DEC-0017 with revisit-as-defect trigger, subprocess test suite proving zero-mutation and fail-closed paths | operator | open |
| RISK-INTEG-01 | Documentation or runtime claims exceed enforced behavior (packaging truth, quarantine, provenance, capture, analysis scope, lifecycle semantics). Phase 1A shipped four such gaps (corrected by DEC-0006); Phase 1A.1 itself then overstated "human-only quarantine release" — the check accepted caller-fabricated objects — and left `#chars=` offsets multilingually undefined (both corrected by DEC-0007); Phase 1B.1 left "blocked on Q-002" language in active skill docs after DEC-0009 had closed Q-002 (corrected in Phase 1B.2); Phase 1B.2 shipped a confirmed judgment error — `contradicted` claims stayed active, so a human resolution could never close a contradiction, and caller-supplied claim arrays let omission imitate resolution with no lineage or audit trail (both corrected by DEC-0012); Phase 1B.2.1 then described the supplied claim array as a "complete revision set" while completeness stayed caller-asserted — omitting an entire independent lineage hid its contradiction with nothing dangling (corrected by DEC-0013: store-derived snapshot membership, digest binding, stale-analysis fail-closed); Phase 1B.2.2 in turn left the read boundary itself unproven — `store.get` validated contract and brand but not that a file's internal artifact_id equals its canonical filename address, so a schema-valid artifact planted under a foreign filename read back under the wrong identity (corrected in Phase 1B.3A under DEC-0014: typed `artifact-address-mismatch` at `store.get`/record reads, snapshot capture fails immediately, regression tests plant mismatched files). | medium | high | a stated guarantee has no failing test; a "quarantine"/"captured"/"zero-dependency"/"human-released"/"resolved" claim without an enforcement path; a coordinate or unit no contract defines; a "contradiction detection" claim without the structured-fact-slot qualifier (DEC-0011 prohibits semantic-detection claims); active docs citing a closed question as open; a lifecycle state whose consumers ignore it | every stated boundary carries a deterministic test that fails without it (production-install smoke, fail-closed quarantine surface + fabricated-approval rejection, canonical-reference rejection, code-point fragment proofs on supplementary-plane text, prose-conflict-produces-no-contradiction proof, contradicted-claim-inactive and lineage fail-closed proofs); claims of human action require an authentication path before they may be written; corrections are appended decisions, never silent rewrites | architecture owner | open |
