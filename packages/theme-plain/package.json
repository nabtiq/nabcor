{
  "name": "@nabcor/theme-plain",
  "version": "0.1.0",
  "description": "Plain — token-only TEST FIXTURE proving the core default fallback (components: {}). Not a product theme, not for client use.",
  "type": "module",
  "sideEffects": ["*.css"],
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css",
    "./package.json": "./package.json"
  },
  "files": ["src"],
  "dependencies": {
    "@nabcor/core": "*"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
